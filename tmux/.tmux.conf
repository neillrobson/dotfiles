bind-key r run "tmux source-file ~/.tmux.conf; stty -ixon; true"

unbind C-b
set -g prefix C-s
bind C-s send-prefix

set -g default-terminal "screen-256color"

set -g status-keys vi
setw -g mode-keys vi

set -s escape-time 0
